// ============================================================
// Определения 18 инструментов AI-агента: схемы для YandexGPT
// function calling + типы аргументов на TypeScript-стороне.
// Реализация вызовов — в src/lib/agent/executor.ts.
//
// Действия, помеченные needsConfirmation: true, никогда не
// выполняются executor'ом напрямую из основного цикла — route.ts
// (/api/assistant) должен вернуть { type: "confirmation_required" }
// и выполнить их только через /api/assistant/confirm после
// явного подтверждения психолога.
// ============================================================
import type { YandexGptTool } from "@/lib/yandexgpt";

export type AgentToolName =
  | "get_clients"
  | "find_client_by_name"
  | "get_client_info"
  | "create_client"
  | "update_client"
  | "search_client_history"
  | "get_test_results"
  | "get_period_summary"
  | "search_knowledge_base"
  | "get_schedule"
  | "get_preferences"
  | "find_available_slots"
  | "create_session"
  | "cancel_session"
  | "send_message_to_client"
  | "send_homework"
  | "send_session_invite"
  | "send_broadcast_message";

/** Инструменты, необратимые по своей природе — требуют подтверждения психолога. */
export const CONFIRMATION_REQUIRED_TOOLS: ReadonlySet<AgentToolName> = new Set([
  "create_client",
  "update_client",
  "create_session",
  "cancel_session",
  "send_message_to_client",
  "send_homework",
  "send_session_invite",
  "send_broadcast_message",
]);

export function toolNeedsConfirmation(name: string): boolean {
  return CONFIRMATION_REQUIRED_TOOLS.has(name as AgentToolName);
}

/**
 * Урезанный набор инструментов для справочных вопросов о платформе
 * (когда selectAssistantModel уже определил — по тем же признакам,
 * что и выбор lite/pro, — что вопрос не касается конкретного клиента
 * или расписания). Экономит на схеме function calling: 18 инструментов
 * в JSON — 8442 символа, эти два — на порядок меньше, а передаются
 * ИМЕННО ТУДА, где остальные 16 физически не могут понадобиться.
 *
 * find_client_by_name оставлен как страховка, а не потому что он нужен
 * для справочных вопросов сам по себе: если эвристика ошиблась и вопрос
 * всё-таки был про конкретного клиента ("а как обстоят дела у Марины" —
 * без явных маркеров, которые ловит modelSelection), модель сможет хотя
 * бы найти клиента и дать частичный осмысленный ответ или адресовать
 * дальнейший вопрос, вместо жёсткого "не могу помочь с этим здесь" —
 * ответ психологу не должен становиться хуже ради экономии на токенах.
 */
export const REFERENCE_ONLY_TOOL_NAMES: ReadonlySet<AgentToolName> = new Set([
  "search_knowledge_base",
  "find_client_by_name",
]);

// ------------------------------------------------------------
// Схемы function calling (JSON Schema под YandexGPT tools[].function.parameters)
// ------------------------------------------------------------
export const AGENT_TOOLS: YandexGptTool[] = [
  // --- Клиенты ---
  {
    function: {
      name: "get_clients",
      description: "Возвращает список всех клиентов психолога (id, имя, статус, запрос, подход).",
      parameters: { type: "object", properties: {} },
    },
  },
  {
    function: {
      name: "find_client_by_name",
      description:
        "Ищет клиента по имени/фамилии (частичное совпадение). Используй первым, когда психолог называет клиента по имени, не UUID (например «что там у Марины»). Один результат — сразу используй его id в следующем вызове (get_client_info и т.д.); несколько — уточни у психолога, кого он имел в виду.",
      parameters: {
        type: "object",
        properties: {
          name_query: { type: "string", description: "Имя, фамилия или их часть, как упомянул психолог" },
        },
        required: ["name_query"],
      },
    },
  },
  {
    function: {
      name: "get_client_info",
      description: "Возвращает подробную информацию о клиенте: профиль, историю сессий, контакты. Требует client_id — если известно только имя, сначала вызови find_client_by_name.",
      parameters: {
        type: "object",
        properties: {
          client_id: { type: "string", description: "UUID клиента" },
        },
        required: ["client_id"],
      },
    },
  },
  {
    function: {
      name: "create_client",
      description: "Создаёт нового клиента. Необратимое действие — требует подтверждения психолога.",
      parameters: {
        type: "object",
        properties: {
          name: { type: "string", description: "Имя клиента" },
          request: { type: "string", description: "Запрос клиента (с чем пришёл)" },
          approach: { type: "string", description: "Терапевтический подход" },
          telegram: { type: "string", description: "Username в Telegram, если известен" },
          phone: { type: "string", description: "Телефон клиента, если известен" },
        },
        required: ["name"],
      },
    },
  },
  {
    function: {
      name: "update_client",
      description: "Обновляет данные существующего клиента. Необратимое действие — требует подтверждения психолога.",
      parameters: {
        type: "object",
        properties: {
          client_id: { type: "string", description: "UUID клиента" },
          fields: {
            type: "object",
            description: "Поля для обновления: name, request, approach, status (active|pause|completed)",
          },
        },
        required: ["client_id", "fields"],
      },
    },
  },

  // --- История и RAG ---
  {
    function: {
      name: "search_client_history",
      description:
        "Ищет фрагменты в истории сессий конкретного клиента. Для СМЫСЛОВЫХ вопросов (что обсуждали, как менялось состояние по теме) заполни query — сработает similarity search по транскриптам. " +
        "Для вопросов про ПОРЯДОК или ПЕРИОД сессий (первая сессия, последняя сессия, первые/последние N встреч, за последние N месяцев, с даты по дату) — " +
        "используй session_position и/или date_from/date_to вместо угадывания через query: они определяют нужные сессии точно, без поиска по смыслу. " +
        "Можно сочетать: например «как менялась злость в первых 3 сессиях» — задай и session_position, и query, чтобы искать по теме именно внутри этих сессий. " +
        "Если ни query, ни session_position/даты не подходят под вопрос — задай хотя бы query.",
      parameters: {
        type: "object",
        properties: {
          client_id: { type: "string", description: "UUID клиента" },
          query: {
            type: "string",
            description: "Поисковый запрос по смыслу (например, «делегирование задач»). Не обязателен, если задан session_position без уточнения темы.",
          },
          session_position: {
            type: "string",
            enum: ["first", "last", "first_n", "last_n"],
            description:
              "«first» — первая сессия, «last» — последняя (самая свежая), «first_n»/«last_n» — первые/последние N сессий (укажи session_position_count). Считается по хронологии сессий клиента, не по дате запроса.",
          },
          session_position_count: {
            type: "integer",
            description: "N для session_position=first_n/last_n (например 3 — «первые 3 сессии»). По умолчанию 3, если не указано.",
          },
          date_from: { type: "string", description: "Начало периода, YYYY-MM-DD (например, для «за последние 3 месяца» — дата 3 месяца назад)." },
          date_to: { type: "string", description: "Конец периода, YYYY-MM-DD." },
        },
        required: ["client_id"],
      },
    },
  },
  {
    function: {
      name: "get_test_results",
      description:
        "Возвращает результаты тестов клиента (методика, балл, максимум, интерпретация, дата) от старых к новым — видна динамика. Используй при вопросах про тесты/баллы/шкалы/динамику («что по тревожности», «какие результаты тестов», «динамика по шкале»).",
      parameters: {
        type: "object",
        properties: {
          client_id: { type: "string", description: "UUID клиента" },
        },
        required: ["client_id"],
      },
    },
  },
  {
    function: {
      name: "get_period_summary",
      description: "Строит суммаризацию работы с клиентом за период: темы, динамика, паттерны.",
      parameters: {
        type: "object",
        properties: {
          client_id: { type: "string", description: "UUID клиента" },
          date_from: { type: "string", description: "Начало периода, YYYY-MM-DD" },
          date_to: { type: "string", description: "Конец периода, YYYY-MM-DD" },
        },
        required: ["client_id", "date_from", "date_to"],
      },
    },
  },
  {
    function: {
      name: "search_knowledge_base",
      description: "Ищет в базе знаний психолога: терапевтические материалы (техники, статьи, протоколы) и справочные материалы о платформе ТОЛК (topic='platform') — часовой пояс, рабочие часы, Telegram/VK, шаблоны протоколов, тарифы и лимиты, экспорт данных. Используй для ЛЮБОГО вопроса о том, как что-то сделать или устроено в самом приложении — не отвечай из общих знаний и не отказывайся сразу.",
      parameters: {
        type: "object",
        properties: {
          query: { type: "string", description: "Поисковый запрос" },
          approach: { type: "string", description: "Опционально: фильтр по терапевтическому подходу" },
        },
        required: ["query"],
      },
    },
  },

  // --- Расписание ---
  {
    function: {
      name: "get_schedule",
      description: "Возвращает сессии психолога за указанный период.",
      parameters: {
        type: "object",
        properties: {
          date_from: { type: "string", description: "Начало периода, YYYY-MM-DD" },
          date_to: { type: "string", description: "Конец периода, YYYY-MM-DD" },
        },
        required: ["date_from", "date_to"],
      },
    },
  },
  {
    function: {
      name: "get_preferences",
      description: "Возвращает сохранённые предпочтения психолога по расписанию (буфер между сессиями, рабочие часы и т.д.).",
      parameters: { type: "object", properties: {} },
    },
  },
  {
    function: {
      name: "find_available_slots",
      description:
        "Находит свободные слоты для сессии с учётом занятости, буфера между сессиями, времени на дорогу и предпочтительных часов/дней.",
      parameters: {
        type: "object",
        properties: {
          duration_minutes: { type: "integer", description: "Длительность сессии в минутах" },
          date_from: { type: "string", description: "Начало диапазона поиска, YYYY-MM-DD" },
          date_to: { type: "string", description: "Конец диапазона поиска, YYYY-MM-DD" },
        },
        required: ["duration_minutes"],
      },
    },
  },

  // --- Сессии ---
  {
    function: {
      name: "create_session",
      description:
        "Создаёт новую сессию с клиентом на конкретное время. Необратимое действие — требует подтверждения психолога.",
      parameters: {
        type: "object",
        properties: {
          client_id: { type: "string", description: "UUID клиента" },
          datetime: { type: "string", description: "Дата и время сессии, ISO 8601" },
          duration_minutes: { type: "integer", description: "Длительность в минутах (по умолчанию 50)" },
        },
        required: ["client_id", "datetime"],
      },
    },
  },
  {
    function: {
      name: "cancel_session",
      description: "Отменяет запланированную сессию. Необратимое действие — требует подтверждения психолога.",
      parameters: {
        type: "object",
        properties: {
          session_id: { type: "string", description: "UUID сессии" },
          reason: { type: "string", description: "Причина отмены (опционально)" },
        },
        required: ["session_id"],
      },
    },
  },

  // --- Коммуникация ---
  {
    function: {
      name: "send_message_to_client",
      description:
        "Готовит сообщение клиенту к отправке в мессенджер. Необратимое действие — требует подтверждения психолога.",
      parameters: {
        type: "object",
        properties: {
          client_id: { type: "string", description: "UUID клиента" },
          text: { type: "string", description: "Текст сообщения" },
          channel: { type: "string", enum: ["telegram", "vk"], description: "Канал отправки" },
        },
        required: ["client_id", "text", "channel"],
      },
    },
  },
  {
    function: {
      name: "send_homework",
      description: "Готовит домашнее задание клиенту к отправке. Необратимое действие — требует подтверждения психолога.",
      parameters: {
        type: "object",
        properties: {
          client_id: { type: "string", description: "UUID клиента" },
          homework_text: { type: "string", description: "Текст домашнего задания" },
        },
        required: ["client_id", "homework_text"],
      },
    },
  },
  {
    function: {
      name: "send_session_invite",
      description: "Готовит отправку ссылки на видеокомнату сессии клиенту. Необратимое действие — требует подтверждения психолога.",
      parameters: {
        type: "object",
        properties: {
          session_id: { type: "string", description: "UUID сессии" },
        },
        required: ["session_id"],
      },
    },
  },
  {
    function: {
      name: "send_broadcast_message",
      description:
        "Массовая рассылка одного сообщения ВСЕМ активным клиентам психолога (например «я в отпуске с 10 по 20 сентября»). Необратимо — требует подтверждения. Используй ТОЛЬКО когда психолог явно просит написать всем/всем активным клиентам; для одного клиента — send_message_to_client.",
      parameters: {
        type: "object",
        properties: {
          text: { type: "string", description: "Текст сообщения, который получит каждый активный клиент" },
        },
        required: ["text"],
      },
    },
  },
];

export const MAX_AGENT_ITERATIONS = 5;

/**
 * Урезанный набор для справочных вопросов о платформе — см. комментарий
 * у REFERENCE_ONLY_TOOL_NAMES выше. Вычисляется от полного AGENT_TOOLS,
 * а не задаётся отдельным литералом, чтобы описания инструментов не
 * дублировались и не расходились между полным и урезанным набором.
 */
export function getReferenceOnlyTools(): YandexGptTool[] {
  return AGENT_TOOLS.filter(t => REFERENCE_ONLY_TOOL_NAMES.has(t.function.name as AgentToolName));
}

export const AGENT_SYSTEM_PROMPT = `Ты профессиональный ассистент практикующего психолога.
Ты знаешь всех его клиентов, их историю и расписание.
Используй только данные из инструментов — не придумывай.
Отвечай кратко и по делу на русском языке.
Учитывай предпочтения психолога при планировании.

Если психолог спрашивает про саму платформу (часовой пояс, рабочие часы,
Telegram/VK, шаблоны протоколов, тарифы и лимиты, экспорт данных,
безопасность данных и т.п.) — не отвечай "это не входит в мой функционал"
или "обратитесь в поддержку" сразу. ОБЯЗАТЕЛЬНО вызови search_knowledge_base
с текстом вопроса ПЕРЕД ответом, даже если кажется, что ты и так знаешь
ответ — у тебя НЕТ достоверных знаний об интерфейсе ТОЛК, и любой ответ "из
общих соображений" психолог не отличит от факта. Слово "протокол" в вопросе
про платформу ("где шаблоны протоколов", "как создать свой шаблон") — это
вопрос об интерфейсе, а не о терапии, тоже требует поиска. Отвечай СТРОГО по
тексту, который вернул search_knowledge_base — не дополняй своими догадками
о шагах, кнопках или разделах, которых там нет.
Если результат содержит "suggestWebSearch": true — база знаний психолога
пуста по теме. Для вопросов о самой платформе при пустом результате советуй
обратиться в поддержку (не искать в интернете — интерфейс ТОЛК там не
найти). Для остальных тем (терапевтические техники, клинические темы,
методики) при "suggestWebSearch": true НЕ говори просто "не знаю" и не
выдумывай — прямо предложи: "В вашей базе знаний пока нет материалов по
этой теме. Хотите чтобы я поискал информацию в интернете?" Дальше сам НЕ
вызывай никакой инструмент — жди ответа психолога, сам веб-поиск идёт
отдельным путём вне этого диалога, только по явному действию психолога в
интерфейсе.

Инструменты, которые только ЧИТАЮТ данные (клиенты, поиск клиента, история
сессий, тесты, срез за период, база знаний, расписание, предпочтения,
слоты) не требуют подтверждения психолога — вызывай их сразу, как только
есть параметры, и НИКОГДА не спрашивай текстом "подтвердите?" или
"продолжить?" перед ними — отменять нечего, а лишний вопрос это лишняя
трата времени психолога. Инструменты, которые СОЗДАЮТ, МЕНЯЮТ или
ОТПРАВЛЯЮТ (клиент, сессия, сообщение, домашнее задание, рассылка),
требуют подтверждения — но оно организовано технически (карточка
"Подтвердить/Отменить" покажется сама после вызова инструмента), спрашивать
об этом текстом не нужно.

Пиши как коллега коллеге в мессенджере, а не как отчёт или методичка. НЕ
используй markdown-разметку (**жирный текст**, заголовки через #, списки с
жирными пунктами) — чат отображает только простой текст, звёздочки и
решётки останутся видны буквально и будут выглядеть как баг. Перечисления —
обычным связным текстом или через тире внутри абзаца. Избегай канцелярита
("в рамках терапии мы можем обратить внимание на следующие моменты") —
говори по-человечески, как если бы делился наблюдением с коллегой.

Психолог почти никогда не знает и не называет UUID клиентов — он говорит
именами ("что там у Марины", «Дмитрий Волков» одним сообщением, или
отвечает на твой вопрос «уточните имя» одним именем без глагола). ЗАПРЕЩЕНО
спрашивать UUID или просить "предоставить эту информацию" — сразу вызови
find_client_by_name с этим именем. Один результат — сразу используй его id
в следующем вызове (get_client_info, get_period_summary и т.д.), не
переспрашивая "это тот человек?". Несколько результатов — кратко перечисли
и уточни, кого он имел в виду; но если психолог сам говорит "у меня только
один клиент с таким именем" — доверяй этому.

Пример: на "поставь сессию с Дмитрием на завтра на 10 часов" — сразу
find_client_by_name("Дмитрий") → один результат → вычисли "завтра" от
текущей даты (указана ниже в системном промпте), собери ISO datetime на
10:00 → сразу create_session с найденным client_id и datetime, НЕ спрашивая
текстом "подтвердите создание сессии" и не переспрашивая дату/время, если
психолог их уже назвал (карточка подтверждения покажется сама). Так же для
любых других относительных формулировок времени ("в пятницу", "через
неделю", "послезавтра в 15:30") — всегда считай от текущей даты из
системного промпта, никогда не спрашивай психолога, какое сегодня число.

Данные из ТВОИХ ЖЕ предыдущих ответов в этом диалоге — не источник истины.
Свободные слоты, расписание, список клиентов, результаты тестов и любые
другие сведения бери ТОЛЬКО свежим вызовом инструмента, даже если только
что отвечал на похожий вопрос — между репликами психолог мог забронировать
время, отменить сессию или поменять рабочие часы, а ты этого не видишь.
Особенно расписание: на "а на четверг?", "а есть окна в пятницу?", "во
сколько последнее свободное время?" ВСЕГДА вызывай find_available_slots
или get_schedule заново с новыми датами, никогда не пересобирай ответ из
уже выведенного списка — повторный вызов инструмента дешевле, чем названное
психологу время, когда он на самом деле занят или не работает.

НИКОГДА не упоминай психологу технические имена инструментов, функций,
параметров или полей базы данных (get_period_summary, find_available_slots,
client_id, source_type и т.п.) — ни в каком виде, включая скобки, курсив
или "техническое примечание". То же для UUID клиента, сессии или любой
другой записи (строка вида "de518170-007b-4caa-…") — никогда не произноси
и не печатай их в ответе психологу, ни как "id клиента", ни внутри
предложения. Если для действия нужен идентификатор клиента — сформулируй
это как обычную просьбу уточнить, о ком речь, ИМЕНЕМ, а не термином "ID"
или "client_id". Про свои текущие или предстоящие действия говори
человеческим языком о результате ("сейчас соберу срез по сессиям за
месяц", "проверю свободные слоты"), а не о том, какую функцию вызываешь.

Если ты нашёл клиента (например через find_client_by_name), но для ответа
нужен инструмент, которого сейчас нет в доступном наборе — НЕ пытайся всё
равно "вызвать" его, не описывай процесс, которого не будет, и не говори
"подождите" без реального действия за этим. Вместо этого честно и коротко
скажи, что лучше задать вопрос прямо, и переспроси, что именно нужно —
например: "Нашёл Ольгу — уточните, пожалуйста, что именно вас интересует:
последнюю сессию, тесты или что-то ещё, и я отвечу отдельным сообщением".
Отвечай сразу одним законченным сообщением — никогда не оставляй ответ на
середине действия с формулировками вроде "подождите" или "сейчас получу",
если фактически дальше вызывать нечего.`;
