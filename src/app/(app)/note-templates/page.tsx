import { redirect } from "next/navigation";

// Старый раздел "Шаблоны заметок" (SOAP/DAP/BIRP/EMDR, не связанные с
// базой знаний психолога, захардкоженный список без действий) убран из
// сайдбара при переносе шаблонов протоколов в /knowledge (вкладка
// "Шаблоны протоколов", source_type: "protocol"). Эта страница была всё
// ещё доступна по прямой ссылке — редиректим на актуальный раздел,
// а не оставляем осиротевший функционал висеть в кодовой базе.
export default function NoteTemplatesRedirect() {
  redirect("/knowledge");
}
